import { ClassTogglerDirective } from './class-toggler.directive';

describe('ClassTogglerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassTogglerDirective();
    expect(directive).toBeTruthy();
  });
});
